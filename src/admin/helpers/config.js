export const NODE_ENV = process.env.NODE_ENV || '';

export const API_BASE_URL = process.env.API_BASE_URL || '';

export const NAME = process.env.NAME || '';

export const VEHICULO_POLL_MS = Number(process.env.VEHICULO_POLL_MS) || 5000;
